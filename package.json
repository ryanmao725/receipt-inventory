{
  "name": "receipt-scanner",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "synth": "pnpm --filter infra synth"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0"
}
